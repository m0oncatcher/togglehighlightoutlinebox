# Toggle Highlight Outline Box

Minecraft Fabric mod that lets you toggle the block outline highlight with a keybind (B for default).

No promises on quick updates but i'll try.

---
To build yourself simply:

`./gradlew build`

Drop the resulting jar from `build/libs/` into your Minecraft `mods/` folder.